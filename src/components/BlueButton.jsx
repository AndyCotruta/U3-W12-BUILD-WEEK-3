import React from "react";

function BlueButton({ text }) {
  return (
    <div className="blue-button-container">
      <span className="blue-button-text">{text}</span>
    </div>
  );
}

export default BlueButton;
